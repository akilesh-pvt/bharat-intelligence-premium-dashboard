name: bharat_intelligence_admin
description: 🚀 Premium Admin Dashboard for Bharat Intelligence with ShadCN Flutter UI
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  # 🎨 ShadCN Flutter - Latest Modern UI Components
  shadcn_flutter: ^0.0.24
  
  # UI & Design Essentials
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.7
  lottie: ^3.1.2
  shimmer: ^3.0.0
  flutter_staggered_grid_view: ^0.7.0
  animations: ^2.0.8
  
  # State Management
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  get_it: ^7.7.0
  
  # Authentication
  google_sign_in: ^6.3.0
  google_sign_in_web: ^0.12.4+4
  
  # Backend & Database
  supabase_flutter: ^2.3.4
  
  # Environment & Config
  flutter_dotenv: ^5.2.1
  
  # Charts & Analytics - Latest Versions
  fl_chart: ^0.68.0
  syncfusion_flutter_charts: ^26.2.11
  
  # Utilities
  intl: ^0.19.0
  connectivity_plus: ^6.1.5
  permission_handler: ^11.4.0
  geolocator: ^12.0.0
  uuid: ^4.4.2
  
  # Navigation
  go_router: ^14.2.7
  
  # Local Storage
  shared_preferences: ^2.2.3
  
  # Network
  dio: ^5.4.3+1
  
  # Image Handling
  cached_network_image: ^3.3.1
  image_picker: ^1.1.2
  
  # Date & Time
  timeago: ^3.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
    - .env